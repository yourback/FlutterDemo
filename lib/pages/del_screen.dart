import 'package:flutter/material.dart';

class DelScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: Text('DelScreen'),
      ),
      body: Center(
        child: Text('DelScreen'),
      ),
    );
  }
}
